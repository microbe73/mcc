int putchar(int c);
int getchar();

int main(){
    int a = getchar();
    putchar(a);
    if(a == 57){
        putchar(58);
    }
    /* Turn this into tic tac toe basically
     * Need:
     * Integer arithmetic stuff, function declarations with types, multiple statements
     * conditionals, loops (basically till part 9)
     * */
}
